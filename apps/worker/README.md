# apps/worker

Worker scaffold for ingestion and enrichment jobs.

Current role in Phase 5:

- Defines execution surfaces for data refresh pipelines.
- Provides a check command for CI health.

Validation command:

- `npm run apps:worker:check`
