{
  "name": "price-dashboard-tooling",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run dashboard:build",
    "test": "npm run dashboard:test",
    "dashboard:dev": "vite",
    "dashboard:build": "vite build && node scripts/check-bundle-boundaries.mjs",
    "dashboard:guard": "node scripts/check-bundle-boundaries.mjs",
    "dashboard:preview": "vite preview",
    "dashboard:test": "vitest run",
    "rigour:init": "node scripts/rigour-init.cjs"
  },
  "unused_allowlist": [
    "rigour-cli-shim"
  ],
  "rigour": {
    "unused_allowlist": [
      "rigour-cli-shim"
    ]
  },
  "dependencies": {
    "d3-geo": "^3.1.1",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "topojson-client": "^3.1.0",
    "world-atlas": "^2.0.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.0",
    "rigour-cli-shim": "file:tools/rigour-cli",
    "vite": "^7.1.12",
    "vitest": "^4.1.6"
  }
}
