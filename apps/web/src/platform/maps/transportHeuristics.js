/**
 * Phase 0 adapter for map heuristics and view-model helpers.
 *
 * Re-export all utilities to guarantee zero drift while callers transition to
 * the platform boundary.
 */

export * from '../../components/cityMapPageUtils.js';
